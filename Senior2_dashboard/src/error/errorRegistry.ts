export const errorRegistry: Record<string, Record<string, string>> = {
    auth: {
      "Invalid Login Credentials": "Invalid Login Credentials",
      "phone is required": "phone is required",
      "password is required": "password is required",
    },
  
  
  };
  