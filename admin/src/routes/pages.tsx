import { ChartBar, User, Users, Truck, FileText, Settings, Cpu, Database, Layers, Box, Command } from "lucide-react";

export const allPages = [
  { name: "Users", path: "/users", icon: Users },

 
];
